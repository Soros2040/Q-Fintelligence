[project]
name = "q-fintelligence-finance-worker"
version = "0.1.0"
requires-python = ">=3.11,<3.12"
dependencies = [
  "pydantic==2.12.5",
]

[project.scripts]
qf-finance-worker = "qf_finance_worker.cli:main"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/qf_finance_worker"]
